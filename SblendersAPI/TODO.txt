-> Autenticação de Input em Todas as Coisas

-> ClienteOnlineController
	-> Put: mandar o email

-> VerificationEmailController
	-> Post: mandar email dnv se ainda n foi mandado

-> PedidosController
	->POST: Disponível para funcionarios para mudar o status do pedido
	->GET (per-id) para clientes e funcionarios, mesma coisa so que pra ver os detalhes do pedido (ingredientes, etc)

-> RestauranteController
	->GET (per-id) retorna os detalhes de um restaurante
	->GET (index) uhh query parms: (range,lat,long) retorna os restaurantes na distancia especificada